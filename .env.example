# Optional — enables the AI enrichment layer
ANTHROPIC_API_KEY=

# Optional — used by --github (provided automatically in GitHub Actions)
GITHUB_TOKEN=
GITHUB_REPOSITORY=owner/repo

# Optional — used by --slack
SLACK_WEBHOOK_URL=
